# Codex 仓库处理规则

以后处理本仓库时，请遵守以下规则。

## 仓库定位

1. 本仓库是 GPT-Image-2 图像提示词案例库，主要用于收集、整理和说明不同图像生成场景下的提示词案例。
2. 处理本仓库任务时，应优先理解案例库用途，避免把它当作普通应用代码仓库处理。

## 优先阅读文件

在新增、修改或整理中文提示词相关内容前，优先读取：

1. `README_zh_image_prompt_guide.md`
2. `PROMPT_SELECTOR_zh.md`
3. 必要时再参考 `README.md`、`README_zh-CN.md` 和其他本地化 README 文件。

## 文件新增与修改规则

1. 新增中文提示词、中文教程、中文模板或中文规则时，尽量写入独立 Markdown 文件。
2. 不要擅自删除、移动或清空 `data/` 和 `images/` 目录中的内容。
3. 不要无关修改 `README.md`、`README_zh-CN.md`、`README_zh_image_prompt_guide.md`、`data/`、`images/`、`LICENSE` 或其他已有文件。
4. 如果任务要求只新增指定文件，必须严格限制变更范围。

## 提示词内容规则

1. 不要使用具体在世艺术家、导演、工作室名称作为风格指令。
2. 遇到用户要求具体在世创作者或工作室风格时，应改写为泛化描述，例如：
   - 原创日系动画电影感
   - 手绘动画背景
   - 电影感构图
   - 高透明度光影
   - 细腻云层
   - 夏日青春氛围
   - 唯美自然风景
3. 处理人物照片时，应强调保留人物身份特征、五官比例、发型、姿态和主要服装特征。
4. 不要把多张人物照片默认合成为一张图。多图输入时默认逐一处理，除非用户明确要求合成、拼图、多人同框或对比图。

## 工作流程规则

1. 修改前先说明计划，明确将新增或修改哪些文件。
2. 修改后给出文件变更摘要，说明新增、修改、删除的文件范围。
3. 默认通过新分支和 Pull Request 提交，不直接修改 `main` 分支。
4. 创建 PR 前应确认变更范围符合用户要求，尤其要检查是否只包含允许的文件。
5. PR 描述应说明本次变更内容和未修改的关键文件或目录。
